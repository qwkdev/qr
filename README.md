# qr
Command Line QR Code Generator

Made in like 10 minutes as a small ```qrcode``` library wrapper

# Install
Clone, Give Execute Permission and Run:
```./install.sh```

# Usage
```qr [data]```
Or optionally to make it smaller:
```qr [data] --small```